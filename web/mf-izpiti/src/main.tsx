import React from "react";
import ReactDOM from "react-dom/client";
import IzpitiApp from "./IzpitiApp";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <IzpitiApp />
  </React.StrictMode>
);
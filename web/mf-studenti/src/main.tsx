import React from "react";
import ReactDOM from "react-dom/client";
import StudentiApp from "./StudentiApp";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <StudentiApp />
  </React.StrictMode>
);